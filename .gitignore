# Daily runtime artifacts — generated by the pipeline, contain fetched news.
# dailynews-report-* / digest_runs/ are the post-rename names; the old rss-report-*
# and runs/ patterns stay because existing clones still have those files on disk.
rss-report-*.md
dailynews-report-*.md
runs/
digest_runs/

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Editor / local tooling
.codex/
.qoder/
.claude/worktrees/
/.obsidian/

# Local planning and review notes
/TASKS.md
/ANDROID_PLAN.md
/android/REVIEW-2026-08-04.md
