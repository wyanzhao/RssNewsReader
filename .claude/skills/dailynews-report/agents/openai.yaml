interface:
  display_name: "DailyNews Report"
  short_description: "Run the DailyNews RSS report workflow"
  default_prompt: "Use $dailynews-report to generate today's DailyNews RSS report and return the fixed-format Top 30 digest."
policy:
  allow_implicit_invocation: false
